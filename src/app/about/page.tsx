import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Page - Next Base",
  description: "About page in Next Base Project",
};

const about = () => {
  return <div>hello</div>;
};

export default about;
